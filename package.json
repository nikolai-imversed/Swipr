{
  "dependencies": {
    "discord.js": "^14.8.0",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  },
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "start": "yarn build && node build/main.js"
  }
}
